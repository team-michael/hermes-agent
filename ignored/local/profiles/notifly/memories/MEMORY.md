Notifly profile grounding: official docs are available through the configured MCP server `notifly_docs` at https://docs.notifly.tech/mcp. Product knowledge is in the private GitHub repo `team-michael/notifly-product-knowledge`; the notifly profile has a GitHub token in its `.env` that can read the repo via GitHub REST API. In this environment, 1Password CLI (`op`) is not installed, and the token worked for REST `gh api` but GraphQL/code-search style `gh search code` returned 401, so use REST contents/trees or another approved retrieval path.