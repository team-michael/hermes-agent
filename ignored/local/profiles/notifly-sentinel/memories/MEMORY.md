CORRECTED 2026-07-20 (refined): bold-syntax dialect is PER-CHANNEL/PER-CONTEXT in this Slack workspace, not global. Confirmed via live evidence: the notifly-sentinel bot's own DM thread required DOUBLE-asterisk **text** for bold (single-asterisk rendered as plain text there), but the actual production channel #notifly-product-report (C0BEJ83P093) uses standard single-asterisk *text* bold — confirmed by other existing recurring reports already posting there successfully with single-asterisk bold, and by the double-asterisk W28 report visibly failing to bold there. Rule: NEVER assume a bold-syntax finding from one chat/DM carries over to a different channel. Before the first formatted send to a NEW channel, either (a) inspect recent existing messages in that channel via conversations_history for an already-working bold pattern to match, or (b) send an isolated test line and confirm. No pipe-table (`| | |`) support confirmed anywhere in this workspace so far — use a text card layout (bold label + `>` blockquote/bullet detail lines) or an image table (matplotlib) for grids.